$("#post_<%= @post.id %> .comments-count").html("コメント数:<%= @post.comments.count %>");
$("#post_<%= @post.id %> .comments").html("<%= j(render 'comments/comment', post: @post) %>");
$("#comment_content").val("");