$("#post_<%= @post.id %> .comments-count").html("<%= @post.comments.count %>");
$("#post_<%= @post.id %> .comments").html("<%= j(render 'comments/comment', post: @post) %>");
$("#comment_content").val("");